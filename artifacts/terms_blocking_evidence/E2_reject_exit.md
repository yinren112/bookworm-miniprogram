# E2 拒绝退出验证

## Modal 文案与按钮
- 标题：提示
- 内容：不同意《用户服务协议》和《隐私政策》将无法使用本服务。
- 确认按钮：退出小程序
- 取消按钮：返回查看

## 行为路径
1. 在协议页点击“拒绝并退出”。
2. 弹出上述 modal。
3. 选择“退出小程序”后调用 `wx.exitMiniProgram()`。

## Devtools 兼容与兜底
- 当 `platform=devtools` 或 `wx.exitMiniProgram` 失败时：
  - 提示“当前环境无法退出，请手动关闭小程序”。
  - 仍停留在协议页，不放行进入业务页面。

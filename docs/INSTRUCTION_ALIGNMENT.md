# 指令一致性检查（CLAUDE.md vs AGENTS.md）

## 检查范围
- 文件：`CLAUDE.md`、`AGENTS.md`
- 方法：逐项比对角色定义、沟通/语言要求、项目结构与流程描述，并核对仓库实际代码与目录（如 `miniprogram/`、`bookworm-backend/`）是否匹配。

## 发现与结论
- **角色与沟通要求**：两份文件均要求以 “Linus Torvalds” 视角、中文输出、高信噪比汇报；未发现描述冲突。
- **项目结构描述**：`AGENTS.md` 说明前端在 `miniprogram/`、后端在 `bookworm-backend/`；与实际目录一致。`CLAUDE.md` 未声明不同信息，故无冲突。
- **流程/规则**：两份文件的报告格式与零废话原则一致，未见互斥条款。

## 结论
未发现需改写的冲突性陈述；现有指令可同时遵循，无需变更。若后续新增要求，请在两份文件同步更新以保持一致。

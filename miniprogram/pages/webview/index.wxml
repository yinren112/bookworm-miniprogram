<!--pages/webview/index.wxml-->
<view class="container page-container">
  
  <!-- Loading -->
  <loading-state wx:if="{{isLoading}}" variant="page" text="加载中..." />

  <!-- Error -->
  <view wx:elif="{{errorMsg}}" class="error-container">
    <error-state title="加载失败" desc="{{errorMsg}}" bind:retry="onRetry" />
  </view>

  <!-- Content (Paper Style) -->
  <card-flat wx:else>
    <view class="content-body">
      <view class="doc-title">{{content.title}}</view>
      <view class="doc-divider"></view>
      <rich-text nodes="{{content.body}}" class="rich-content"></rich-text>
    </view>
  </card-flat>

</view>

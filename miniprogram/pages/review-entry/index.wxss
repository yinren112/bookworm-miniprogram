/* pages/review-entry/index.wxss */
.container {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-bg-soft);
}

.loading-text {
  font-size: 28rpx;
  color: var(--color-text-light);
}
